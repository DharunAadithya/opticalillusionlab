---
interface Props {
  isCard?: boolean;
}

const { isCard = false } = Astro.props;
---

<div class="penrose-container flex flex-col items-center w-full select-none" data-is-card={isCard ? "true" : "false"}>
  <div class="relative flex flex-col items-center justify-center bg-white rounded-xl overflow-hidden border border-slate-200 shadow-inner w-full min-h-[380px] p-6 pb-20">
    
    <!-- SVG Area (300x300 px) -->
    <svg class="w-full max-w-[300px] h-auto aspect-square select-none overflow-visible" viewBox="0 0 300 300">
      <defs>
        <!-- Subtle drop shadow filter -->
        <filter id="drop-shadow-penrose" x="-20%" y="-20%" width="140%" height="140%">
          <feDropShadow dx="2" dy="4" stdDeviation="4" flood-color="#000000" flood-opacity="0.25" />
        </filter>
      </defs>

      <!-- Impossible Penrose Triangle with Drop Shadow -->
      <g class="penrose-g origin-center" filter="url(#drop-shadow-penrose)">
        <!-- 
          Grayscale Scheme:
          - TOP faces: lightest gray #FFFFFF
          - LEFT faces: medium gray #8C8C8C
          - RIGHT faces: darkest gray #2E2E2E
        -->
        <!-- Limb 1 Left face -->
        <polygon points="260.00,213.51 241.67,245.26 150.00,86.49 168.33,54.74" fill="#8C8C8C" stroke="#8C8C8C" stroke-width="0.5" />
        <!-- Limb 1 Right face -->
        <polygon points="205.00,245.26 113.33,86.49 150.00,86.49 241.67,245.26" fill="#2E2E2E" stroke="#2E2E2E" stroke-width="0.5" />
        <!-- Limb 2 Top face -->
        <polygon points="131.67,54.74 40.00,213.51 76.67,213.51 168.33,54.74" fill="#FFFFFF" stroke="#FFFFFF" stroke-width="0.5" />
        <!-- Limb 2 Left face -->
        <polygon points="186.67,86.49 95.00,245.26 76.67,213.51 168.33,54.74" fill="#8C8C8C" stroke="#8C8C8C" stroke-width="0.5" />
        <!-- Limb 3 Top face -->
        <polygon points="58.33,181.75 40.00,213.51 223.33,213.51 241.67,181.75" fill="#FFFFFF" stroke="#FFFFFF" stroke-width="0.5" />
        <!-- Limb 3 Right face -->
        <polygon points="58.33,245.26 40.00,213.51 223.33,213.51 241.67,245.26" fill="#2E2E2E" stroke="#2E2E2E" stroke-width="0.5" />
      </g>
    </svg>

    <!-- Controls Panel -->
    <div class="absolute bottom-3 left-0 right-0 flex justify-center gap-2 px-4 z-20">
      <button class="btn-toggle-grid bg-[#7C3AED] hover:bg-[#6D28D9] text-white text-[10px] font-black px-4 py-2 rounded-lg border-none transition-colors shadow cursor-pointer">
        Show Grid
      </button>
    </div>
  </div>
</div>

<script>
  function initPenrose() {
    const containers = document.querySelectorAll('.penrose-container');
    containers.forEach(container => {
      if (container.getAttribute('data-initialized') === 'true') return;
      container.setAttribute('data-initialized', 'true');

      const btnGrid = container.querySelector('.btn-toggle-grid');
      const grid = container.querySelector('.penrose-grid');

      let gridActive = false;

      btnGrid?.addEventListener('click', (e) => {
        e.preventDefault();
        e.stopPropagation();
        gridActive = !gridActive;

        if (grid) {
          grid.classList.toggle('hidden', !gridActive);
        }
        if (btnGrid) {
          btnGrid.textContent = gridActive ? 'Hide Grid' : 'Show Grid';
        }
      });
    });
  }

  initPenrose();
  document.addEventListener('astro:page-load', initPenrose);
</script>
