---
// src/pages/illusions/necker-cube.astro
import Layout from '../../layouts/Layout.astro';

// Math Helper: draws an SVG wedge from (cx, cy) out to radius r
function getWedgePath(cx: number, cy: number, r: number, startAngle: number, endAngle: number) {
  const rad = Math.PI / 180;
  const x1 = cx + r * Math.cos(startAngle * rad);
  const y1 = cy + r * Math.sin(startAngle * rad);
  const x2 = cx + r * Math.cos(endAngle * rad);
  const y2 = cy + r * Math.sin(endAngle * rad);
  const largeArcFlag = endAngle - startAngle <= 180 ? 0 : 1;
  return `M ${cx},${cy} L ${x1},${y1} A ${r},${r} 0 ${largeArcFlag},1 ${x2},${y2} Z`;
}
---

<Layout 
  title="Necker Cube — Interactive Optical Illusion | Optical Illusion Lab" 
  description="A wireframe cube with no depth cues. Watch it flip its front and back faces in your mind."
>
  <div class="py-12 px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto space-y-12">
    
    <!-- Breadcrumb back navigation to Section -->
    <nav class="flex text-xs font-black uppercase tracking-wider text-slate-400 space-x-2 pb-2 border-b border-indigo-500/10">
      <a href="/" class="hover:text-white transition-colors">Home</a>
      <span>/</span>
      <a href="/hidden" class="hover:text-white transition-colors">Hidden & Ambiguous</a>
      <span>/</span>
      <span class="text-purple-400">Necker Cube</span>
    </nav>

    <!-- Hero Header Section -->
    <div class="flex flex-col md:flex-row md:items-center justify-between gap-6 pb-6">
      <div class="space-y-3">
        <div class="flex items-center space-x-2">
          <span class="text-xs font-black px-2.5 py-0.5 rounded-full bg-indigo-500/10 text-indigo-400 border border-indigo-500/20 uppercase tracking-wider">Ambiguous Gestalt</span>
          <span class="text-xs font-black px-2.5 py-0.5 rounded-full bg-purple-500/10 text-purple-400 border border-purple-500/20 uppercase tracking-wider">🟡 Medium</span>
        </div>
        <h1 class="font-display text-4xl sm:text-5xl font-black tracking-tight text-slate-900 dark:text-white">
          Necker Cube
        </h1>
        <p class="text-slate-600 dark:text-slate-350 text-base max-w-2xl font-medium">
          A wireframe cube with no depth cues. Watch it flip its front and back faces in your mind.
        </p>
      </div>

      <!-- Share Button -->
      <button 
        id="share-illusion-page-btn"
        class="relative shrink-0 flex items-center space-x-2 rounded-2xl bg-gradient-to-r from-pink-500 via-purple-600 to-indigo-600 hover:scale-105 text-white font-black text-sm px-6 py-3.5 transition-all shadow-lg shadow-purple-500/10"
      >
        <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 20 20"><path d="M15 8a3 3 0 1 0-2.977-2.63l-4.94 2.47a3 3 0 1 0 0 4.319l4.94 2.47a3 3 0 1 0 .895-1.789l-4.94-2.47a3.027 3.027 0 0 0 0-.74l4.94-2.47C13.456 7.68 14.19 8 15 8Z"/></svg>
        <span>Share Page</span>
        <span id="page-share-toast" class="absolute bottom-full right-1/2 translate-x-1/2 mb-2 px-2.5 py-1 text-[10px] font-bold text-white bg-purple-650 rounded-lg shadow-md opacity-0 pointer-events-none transition-opacity duration-200 whitespace-nowrap">Link Copied! ✓</span>
      </button>
    </div>

    <!-- LARGE INTERACTIVE DEMO (600px wide, centered) -->
    <div class="flex flex-col items-center justify-center py-6 border-b border-indigo-500/10">
      <div class="w-full max-w-[600px] mx-auto space-y-4">
        <div id="necker-illusion" class="relative flex flex-col items-center justify-center h-96 w-full bg-slate-100 dark:bg-slate-950/40 rounded-xl overflow-hidden border border-indigo-500/10">
          <svg class="w-full h-80 p-6 select-none" viewBox="0 0 200 200">
            <!-- Back face -->
            <rect class="necker-back stroke-slate-400 dark:stroke-slate-500 stroke-[2] fill-none" x="40" y="40" width="80" height="80" />
            <!-- Front face -->
            <rect class="necker-front stroke-slate-700 dark:stroke-slate-200 stroke-[3.5] fill-none" x="80" y="80" width="80" height="80" />
            <!-- Connection lines -->
            <line class="necker-line stroke-slate-500 stroke-[2]" x1="40" y1="40" x2="80" y2="80" />
            <line class="necker-line stroke-slate-500 stroke-[2]" x1="120" y1="40" x2="160" y2="80" />
            <line class="necker-line stroke-slate-500 stroke-[2]" x1="40" y1="120" x2="80" y2="160" />
            <line class="necker-line stroke-slate-500 stroke-[2]" x1="120" y1="120" x2="160" y2="160" />
            
            <!-- Shading layers (hidden by default) -->
            <polygon class="necker-shade-a fill-purple-600/30 opacity-0 transition-opacity duration-300" points="40,40 120,40 120,120 40,120" />
            <polygon class="necker-shade-b fill-blue-600/30 opacity-0 transition-opacity duration-300" points="80,80 160,80 160,160 80,160" />
          </svg>
          <div class="absolute bottom-4 flex space-x-2 bg-slate-900/90 border border-white/10 rounded-xl p-1">
            <button data-face="normal" class="necker-btn text-xs font-bold px-3 py-1.5 rounded-lg bg-purple-600 text-white transition-all">Ambiguous</button>
            <button data-face="a" class="necker-btn text-xs font-bold px-3 py-1.5 rounded-lg text-slate-400 hover:text-white transition-all">Front Left</button>
            <button data-face="b" class="necker-btn text-xs font-bold px-3 py-1.5 rounded-lg text-slate-400 hover:text-white transition-all">Front Right</button>
          </div>
        </div>
    
        <div class="text-center text-xs text-slate-500 dark:text-slate-400 italic">
          Use the controllers inside the display card to interact with the visual triggers.
        </div>
        <div class="text-center text-xs text-slate-500 dark:text-slate-400 italic font-semibold">
          Use the controllers inside the display card to interact with the visual triggers.
        </div>
      </div>
    </div>

    <!-- Content Split Layout -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-12 pt-4">
      
      <!-- Left 2-Columns: Science and Prompt -->
      <div class="lg:col-span-2 space-y-10">
        <!-- EXPERIENCE IT FIRST prompt -->
        <section class="space-y-4">
          <h2 class="font-display text-2xl font-black text-slate-900 dark:text-white flex items-center space-x-2">
            <span class="text-purple-500">🎮</span>
            <span>EXPERIENCE IT FIRST</span>
          </h2>
          <div class="bg-indigo-950/20 border-2 border-indigo-500/15 rounded-3xl p-6 text-slate-700 dark:text-slate-300">
            <p class="font-bold text-sm sm:text-base text-slate-200">Before reading the neuroscience explanation below, take a moment to interact with the demo above:</p>
            <ul class="list-disc pl-5 mt-3 space-y-2 text-xs sm:text-sm font-medium">
              <li>How does the visual change when you move your eyes or look at different parts of the screen?</li>
              <li>Use the slider or toggle buttons to reveal the actual geometric layout. Did it match what your eyes predicted?</li>
              <li>Pay attention to whether you can consciously force your brain to switch between interpretations.</li>
            </ul>
          </div>
        </section>

        <!-- THE SCIENCE (400 words) -->
        <section class="space-y-4">
          <h2 class="font-display text-2xl font-black text-slate-900 dark:text-white flex items-center space-x-2">
            <span class="text-purple-500">🧠</span>
            <span>THE SCIENCE</span>
          </h2>
          <div class="prose prose-indigo dark:prose-invert text-slate-700 dark:text-slate-300 leading-relaxed space-y-4 text-xs sm:text-sm font-semibold text-justify">
            <p>The Necker Cube is a famous bistable line drawing of a 3D wireframe cube. Because the drawing has no overlapping lines, shadows, or depth cues, it is physically ambiguous. The brain cannot determine which of the two overlapping squares is the front face and which is the back face.
    
    To resolve this, the visual cortex alternates between two equally plausible 3D hypotheses. In one state, the lower-left square is seen as the front face (meaning you are looking at the cube from above). In the other state, the upper-right square is seen as the front face (viewing the cube from below). This bistable perceptual switching occurs in V2, V4, and parietal areas. Shading one face immediately breaks the ambiguity, forcing a single interpretation.</p>
          </div>
        </section>
      </div>

      <!-- Right Column: Fun Facts and Try This -->
      <div class="space-y-8">
        <!-- FUN FACTS -->
        <section class="rounded-3xl border border-indigo-950/20 bg-slate-900/60 p-6 space-y-4 shadow-xl">
          <h3 class="font-display text-lg font-black text-slate-900 dark:text-white flex items-center space-x-2">
            <span class="text-purple-500">💡</span>
            <span>FUN FACTS</span>
          </h3>
          <ul class="space-y-4 text-xs sm:text-sm text-slate-600 dark:text-slate-300 font-semibold">
            
              <li class="flex items-start space-x-2">
                <span class="text-purple-500 font-black">•</span>
                <span>Louis Albert Necker noticed the flipping effect while studying crystalline structures under a microscope in 1832.</span>
              </li>
            
              <li class="flex items-start space-x-2">
                <span class="text-purple-500 font-black">•</span>
                <span>With practice, you can consciously control the flip by focusing on specific vertices (corners) of the cube.</span>
              </li>
            
              <li class="flex items-start space-x-2">
                <span class="text-purple-500 font-black">•</span>
                <span>The Necker Cube is a standard test in cognitive neuroscience to study visual awareness and attention switching.</span>
              </li>
            
              <li class="flex items-start space-x-2">
                <span class="text-purple-500 font-black">•</span>
                <span>Adding color or shading to the front face stops the bistable switching immediately by resolving the depth coordinates.</span>
              </li>
            
          </ul>
        </section>

        <!-- TRY THIS AT HOME -->
        <section class="rounded-3xl border border-indigo-950/20 bg-slate-900/60 p-6 space-y-4 shadow-xl">
          <h3 class="font-display text-lg font-black text-slate-900 dark:text-white flex items-center space-x-2">
            <span class="text-purple-500">🧪</span>
            <span>TRY THIS AT HOME</span>
          </h3>
          <p class="text-xs sm:text-sm text-slate-650 dark:text-slate-300 leading-relaxed font-semibold">
            Draw a Necker Cube on a sheet of paper. Stare at the central intersection. Try to blink quickly or focus on the top-right corner. Watch the cube flip its perspective, proving how your brain constructs 3D structures from 2D lines.
          </p>
        </section>

        <!-- WHO DISCOVERED IT -->
        <section class="rounded-3xl border border-indigo-950/20 bg-slate-900/60 p-6 space-y-4 shadow-xl">
          <h3 class="font-display text-lg font-black text-slate-900 dark:text-white flex items-center space-x-2">
            <span class="text-purple-500">📜</span>
            <span>WHO DISCOVERED IT</span>
          </h3>
          <div class="text-xs sm:text-sm text-slate-650 dark:text-slate-300 leading-relaxed font-semibold space-y-2">
            <p><strong>Discoverer:</strong> Louis Albert Necker (1832)</p>
            <p>Swiss crystallographer Louis Albert Necker was drawing rhomboidal crystal structures. He noticed that the drawings would suddenly flip their orientation on the page, looking like they were pointing in a different direction. He published this finding, creating the first documented bistable 3D wireframe illusion.</p>
          </div>
        </section>
      </div>

    </div>

    <!-- SEO content: 300 words naturally written -->
    <section class="border-t border-indigo-500/10 pt-12 space-y-4 max-w-4xl mx-auto">
      <h3 class="font-display text-lg font-black text-white uppercase tracking-wider text-center">Educational Resources & History</h3>
      <p class="text-xs sm:text-sm text-slate-350 leading-relaxed text-justify font-semibold">
        Necker Cube bistable optical illusion explanation. Learn about Louis Albert Necker's 1832 wireframe crystal drawing, how the visual cortex resolves ambiguous 3D depth, and test our interactive face-shading cube widget.
      </p>
    </section>

    <!-- RELATED ILLUSIONS -->
    <section class="border-t border-indigo-500/10 pt-12 space-y-6">
      <h2 class="font-display text-2xl font-black text-slate-900 dark:text-white text-center">Related Illusions</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        
      <a 
        href="/illusions/spinning-dancer" 
        class="group rounded-xl border border-indigo-950/20 bg-white dark:bg-slate-900/60 p-5 shadow-lg hover:border-purple-500/40 hover:-translate-y-1 hover:shadow-purple-500/10 transition-all duration-300"
      >
        <div class="text-[10px] font-bold text-purple-400 uppercase tracking-wide">Related Illusion</div>
        <h4 class="font-display text-base font-bold text-slate-900 dark:text-white mt-1 group-hover:text-purple-400 transition-colors">
          Spinning Dancer &rarr;
        </h4>
      </a>
    

      <a 
        href="/illusions/rubins-vase" 
        class="group rounded-xl border border-indigo-950/20 bg-white dark:bg-slate-900/60 p-5 shadow-lg hover:border-purple-500/40 hover:-translate-y-1 hover:shadow-purple-500/10 transition-all duration-300"
      >
        <div class="text-[10px] font-bold text-purple-400 uppercase tracking-wide">Related Illusion</div>
        <h4 class="font-display text-base font-bold text-slate-900 dark:text-white mt-1 group-hover:text-purple-400 transition-colors">
          Rubins Vase &rarr;
        </h4>
      </a>
    

      <a 
        href="/illusions/penrose-triangle" 
        class="group rounded-xl border border-indigo-950/20 bg-white dark:bg-slate-900/60 p-5 shadow-lg hover:border-purple-500/40 hover:-translate-y-1 hover:shadow-purple-500/10 transition-all duration-300"
      >
        <div class="text-[10px] font-bold text-purple-400 uppercase tracking-wide">Related Illusion</div>
        <h4 class="font-display text-base font-bold text-slate-900 dark:text-white mt-1 group-hover:text-purple-400 transition-colors">
          Penrose Triangle &rarr;
        </h4>
      </a>
    
      </div>
    </section>

  </div>

  <script>
    // Share Page URL
    const sharePageBtn = document.getElementById('share-illusion-page-btn');
    const pageToast = document.getElementById('page-share-toast');

    sharePageBtn?.addEventListener('click', () => {
      const pageUrl = window.location.href;
      navigator.clipboard.writeText(pageUrl).then(() => {
        if (pageToast) {
          pageToast.classList.remove('opacity-0');
          pageToast.classList.add('opacity-100');
          setTimeout(() => {
            pageToast.classList.remove('opacity-100');
            pageToast.classList.add('opacity-0');
          }, 2000);
        }
      });
    });

    // Handle Global Speed Mult
    const checkSpeed = () => {
      const level = localStorage.getItem('speed-multiplier-level') || '5';
      let multiplier = 1.0;
      const parsed = parseInt(level);
      if (parsed <= 5) {
        multiplier = 0.2 + (parsed - 1) * 0.2;
      } else {
        multiplier = 1.0 + (parsed - 5) * 0.4;
      }
      document.documentElement.style.setProperty('--speed-multiplier', multiplier.toString());
    };
    checkSpeed();
    window.addEventListener('storage', checkSpeed);

    // Prevent propagation on inner interactive controllers so we don't trigger parent clicks (if wrapped)
    const innerControls = document.querySelectorAll('.interactive-demo-card button, .interactive-demo-card input, .interactive-demo-card select');
    innerControls.forEach(ctrl => {
      ctrl.addEventListener('click', (e) => e.stopPropagation());
    });

    // Local script logic from original file
    // Share Page Link copying
    

    // Custom CSS animations variable bindings
    
    
    // Embed specific illusion script logic
    
      const neckerContainer = document.getElementById('necker-illusion');
      const neckerBtns = neckerContainer?.querySelectorAll('.necker-btn');
      const shadeA = neckerContainer?.querySelector('.necker-shade-a');
      const shadeB = neckerContainer?.querySelector('.necker-shade-b');

      neckerBtns?.forEach(btn => {
        btn.addEventListener('click', (e) => {
          neckerBtns.forEach(b => {
            b.classList.remove('bg-purple-600', 'text-white');
            b.classList.add('text-slate-400', 'hover:text-white');
          });

          const target = e.target as HTMLButtonElement;
          target.classList.remove('text-slate-400', 'hover:text-white');
          target.classList.add('bg-purple-600', 'text-white');
          const face = target.getAttribute('data-face');

          if (face === 'a') {
            shadeA?.classList.remove('opacity-0');
            shadeA?.classList.add('opacity-100');
            shadeB?.classList.remove('opacity-100');
            shadeB?.classList.add('opacity-0');
          } else if (face === 'b') {
            shadeB?.classList.remove('opacity-0');
            shadeB?.classList.add('opacity-100');
            shadeA?.classList.remove('opacity-100');
            shadeA?.classList.add('opacity-0');
          } else {
            shadeA?.classList.remove('opacity-100');
            shadeA?.classList.add('opacity-0');
            shadeB?.classList.remove('opacity-100');
            shadeB?.classList.add('opacity-0');
          }
        });
      });
  </script>
</Layout>
